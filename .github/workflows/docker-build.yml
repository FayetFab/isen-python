name: Build, Push and Scan Docker Image

on: [push, pull_request]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ vars.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ${{ vars.DOCKERHUB_USERNAME }}/myapp:dev-${{ github.sha }} .

      - name: Push Docker image
        run: |
          docker push ${{ vars.DOCKERHUB_USERNAME }}/myapp:dev-${{ github.sha }}

  security-scan:
    needs: docker
    permissions:
      contents: read
      security-events: write
      actions: read
    runs-on: ubuntu-latest

    steps:
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        continue-on-error: true
        with:
          image-ref: "${{ vars.DOCKERHUB_USERNAME }}/myapp:dev-${{ github.sha }}"
          format: "template"
          template: "@/contrib/sarif.tpl"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH"

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: "trivy-results.sarif"

# name: Build & Push Docker Image + Security Scan 

# on:
#   push:
#     branches: [ part1 ]
#   pull_request:
#     branches: [ part1 ]

# jobs:
#   build-and-scan:
#     runs-on: ubuntu-latest
#     name: Docker Build & Sécu Scan

#     steps:
#       - name: Checkout du code
#         uses: actions/checkout@v4

#       - name: Connexion à DockerHub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ vars.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}
        
#       - name: Build de l’image Docker 
#         uses: docker/build-push-action@v3
#         with:
#           context: .
#           push: true
#           tags: ${{ vars.DOCKERHUB_USERNAME }}/my-app:dev-${{ github.sha }}

#       - name: Push de l’image vers Docker Hub
#         run: |
#           docker push ${{ vars.DOCKERHUB_USERNAME }}/my-app:dev-${{ github.sha }}
#       - name: Scan de sécurité avec Trivy
#         uses: aquasecurity/trivy-action@master
#         with:
#           image-ref: '${{ vars.DOCKERHUB_USERNAME }}/my-app:dev-${{ github.sha }}'
#           format: 'table'
#           vuln-type: 'os,library'
#           severity: 'CRITICAL,HIGH'
#         continue-on-error: true

# Update docker-build.yml · FayetFab/isen-python@6c1a16b